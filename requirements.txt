# ── LLM & Routing ─────────────────────────────────────────────────────────────
groq>=0.9.0                    # AsyncGroq client (Phase 1 upgrade)
sentence-transformers>=2.3.0
lightgbm>=4.0.0
scikit-learn>=1.4.0
tiktoken>=0.7.0
joblib>=1.3.0

# ── RAG & Retrieval ───────────────────────────────────────────────────────────
langchain>=0.1.0
langchain-community>=0.0.20
langchain-text-splitters>=0.0.1
# chromadb removed — using Qdrant Cloud only
qdrant-client>=1.9.0               # Qdrant Cloud client (Phase 5 - Vector DB)
langchain-qdrant>=0.1.0            # LangChain ↔ Qdrant adapter
rank-bm25>=0.2.2
pypdf>=3.17.0

# ── API & Web ─────────────────────────────────────────────────────────────────
fastapi>=0.109.0
uvicorn[standard]>=0.27.0      # includes uvloop for maximum async throughput
pydantic>=2.5.0
pydantic-settings>=2.1.0
slowapi>=0.1.9
streamlit>=1.31.0
plotly>=5.18.0
pandas>=2.1.4

# ── Database (Supabase-compatible) ────────────────────────────────────────────
sqlalchemy>=2.0.25
asyncpg>=0.29.0                # async PostgreSQL driver for Supabase (Phase 3)
aiosqlite>=0.20.0              # async SQLite driver for local dev (Phase 3)
python-dotenv>=1.0.0

# ── Redis (Upstash-compatible) ────────────────────────────────────────────────
redis>=5.0.0                   # includes redis.asyncio for Upstash (Phase 3)

# ── Utilities ─────────────────────────────────────────────────────────────────
pyyaml>=6.0.1
tqdm>=4.66.1
numpy>=1.26.0
