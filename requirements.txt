# ── LLM & Routing ──────────────────────────────────────────────────────────────
openai>=1.0.0
lightgbm>=4.0.0
scikit-learn>=1.4.0
joblib>=1.3.0

# ── RAG & Retrieval ─────────────────────────────────────────────────────────────
langchain>=0.1.0
langchain-community>=0.0.20
langchain-text-splitters>=0.0.1
qdrant-client>=1.9.0
langchain-qdrant>=0.1.0
fastembed>=0.2.0

# ── API & Web ───────────────────────────────────────────────────────────────────
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
slowapi>=0.1.9
streamlit>=1.31.0
plotly>=5.18.0
pandas>=2.1.4

# ── Database (Supabase + Alembic) ───────────────────────────────────────────────
sqlalchemy>=2.0.25
psycopg2-binary>=2.9.9
alembic>=1.13.0
python-dotenv>=1.0.0

# ── Redis (Upstash-compatible) ───────────────────────────────────────────────────
redis>=5.0.0

# ── Utilities ───────────────────────────────────────────────────────────────────
pyyaml>=6.0.1
tqdm>=4.66.1
numpy>=1.26.0

# ── Observability (OpenTelemetry + LangFuse) ────────────────────────────────────
opentelemetry-sdk>=1.24.0
opentelemetry-instrumentation-fastapi>=0.45b0
opentelemetry-exporter-otlp-proto-grpc>=1.24.0
opentelemetry-exporter-otlp-proto-http>=1.24.0
langfuse>=2.9.0

# ── Evaluation (offline only — not loaded by the live API) ──────────────────────

tenacity>=8.4.1
PyJWT>=2.8.0
aiohttp>=3.9.0
langchain-huggingface>=0.0.6
